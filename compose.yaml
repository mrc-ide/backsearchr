services:
  backsearchr:
    platform: ${BACKSEARCHR_PLATFORM:-linux/amd64}
    build:
      context: .
      dockerfile: Containerfile
    image: localhost/backsearchr:local
    pull_policy: never
    working_dir: /work
    volumes:
      - ./data/in:/work/data/in
      - ./data/out:/work/data/out
    command: ["--help"]
